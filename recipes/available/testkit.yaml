apiVersion: slax-kitchen/v1
kind: Recipe
metadata:
  name: testkit
  summary: Print facts about the assembled system to the console, so a boot test can assert them
  tags: [testing, rootcopy]
compat:
  flavours: [debian, slackware]
  arch: [32bit, 64bit]
  slax: ">=12.0.0"
  privilege: none
vars:
  # Files to report on. Each becomes one `file <path>: ...` line. Defaults cover what the
  # branding, locale, ssh and TLS recipes change, so `kitchen apply <recipe> testkit`
  # boot-proves that recipe's artifacts reached the union.
  report: >-
    /etc/hostname /etc/slax-version /etc/timezone /etc/localtime /root/.xinitrc
    /etc/ssl/cert.pem /var/lib/connman/kitchen.config
    /etc/systemd/system/multi-user.target.wants/ssh.service /etc/rc.d/rc.local
  # A persistence probe, off unless a profile sets it. On the first boot of a pair the
  # file is absent and gets created; on the second it must still be there. That is the
  # whole of the two-boot persistence assertion -- and it lives here rather than in a
  # recipe of its own because testkit is already the "print facts a boot test can assert"
  # recipe, and a new recipe would cost a cookbook page, an index entry and two prose
  # counts for one `if`.
  #
  # It writes into the UNION, which is the point: with `perch` on, the union's writable
  # branch is the perch session, so the file survives a reboot. Without perch the branch
  # is tmpfs and it cannot -- which is the negative case the test needs.
  marker: ""
steps:
  # WHY A ROOTCOPY PREINIT AND NOT A BUNDLE. Instrumenting a boot normally means building
  # a bundle, which means mksquashfs and a repack of the ISO for every change to the
  # instrumentation. rootcopy needs neither: livekitlib SOURCES this file just before
  # change_root, so editing one small script is the whole edit loop.
  #
  # WHY BEFORE change_root RATHER THAN AFTER BOOT. A post-boot reporter needs a systemd
  # unit on Debian and an rc script on Slackware -- two mechanisms, two failure modes,
  # and nothing at all if the desktop does not start. user_preinit runs on both flavours,
  # identically, with the union fully assembled at $1. Every fact below is a property of
  # the ASSEMBLED FILESYSTEM, which is exactly what a recipe changes.
  #
  # The union type is the headline. A kernel without aufs silently downgrades to
  # overlayfs, `slax activate` stops working, and nothing says why -- so print it.
  #
  # Output goes to stderr, which is where livekitlib's own messages go, and therefore to
  # the serial console when `console=ttyS0` is on the cmdline. tests/boot/qemu_boot.py
  # --expect can then match on these lines.
  - verb: rootcopy.preinit
    script: |
      # Sourced, not executed: no shebang semantics, POSIX only, and `exit` would end the
      # BOOT rather than this script. Everything below is wrapped accordingly.
      UNION="$1"

      (
          echo "### TESTKIT BEGIN"

          # aufs or overlayfs -- the silent failure mode for any kernel swap.
          _u=$(awk '$2=="/memory/union"{print $3; exit}' /proc/mounts 2>/dev/null)
          [ -n "$_u" ] || _u=$(awk '$2=="'"$UNION"'"{print $3; exit}' /proc/mounts 2>/dev/null)
          echo "union: ${_u:-unknown}"

          echo "bundles: $(ls /memory/bundles 2>/dev/null | tr '\n' ' ')"
          echo "kernel: $(uname -r 2>/dev/null)"

          # busybox reports its own version on the banner line; a replaced one shows here.
          echo "busybox: $(busybox 2>&1 | head -1 | cut -d' ' -f1-2)"

          # Debian's package database is ONE FILE, so in a union the topmost bundle
          # that ships one wins outright and every copy below it vanishes. A bundle
          # built from too short a stack once replaced 600 packages with 299, silently.
          # Counting here -- in the ASSEMBLED filesystem, which is the only place the
          # answer is real -- turns that from a boot-time surprise into an assertion.
          if [ -f "$UNION/var/lib/dpkg/status" ]; then
              echo "dpkg-status: $(grep -c '^Package: ' "$UNION/var/lib/dpkg/status") packages"
          else
              echo "dpkg-status: none (Slackware packs one file per package)"
          fi

          # Empty unless a profile sets `marker`, and then deliberately noisy: a boot
          # test asserts on `absent` for run 1 and `present` for run 2. The line is
          # excluded from golden comparison (GOLDEN_VOLATILE in qemu_boot.py) precisely
          # because it is SUPPOSED to differ between the two.
          if [ -n "{{marker}}" ]; then
              if [ -f "$UNION{{marker}}" ]; then
                  echo "perch-marker: present, written $(cat "$UNION{{marker}}" 2>/dev/null)"
              else
                  echo "perch-marker: absent, creating"
                  mkdir -p "$(dirname "$UNION{{marker}}")" 2>/dev/null
                  date -u '+%Y-%m-%dT%H:%M:%SZ' > "$UNION{{marker}}" 2>/dev/null \
                      || echo "perch-marker: WRITE FAILED"
                  # SYNC, AND IT IS NOT DEFENSIVE. Measured 2026-09-16: without it the
                  # two-boot persistence test failed with the marker absent on BOTH
                  # boots, while the serial log showed "Activating native persistent
                  # changes for session #1" on each -- the mount and the session were
                  # right all along, and the write was sitting in page cache when the
                  # harness stopped the guest. The harness stops as soon as its
                  # expectations appear, which here was four seconds in, so nothing
                  # unflushed can ever be observed by the next boot.
                  sync 2>/dev/null
                  echo "perch-marker: synced"
              fi
          fi

          for f in {{report}}; do
              if [ -L "$UNION$f" ]; then
                  echo "file $f: symlink -> $(readlink "$UNION$f")"
              elif [ -d "$UNION$f" ]; then
                  echo "file $f: directory"
              elif [ -f "$UNION$f" ]; then
                  # One line of content for short text files, a size for anything else:
                  # enough to prove a recipe landed without dumping a CA bundle to serial.
                  _sz=$(wc -c < "$UNION$f" 2>/dev/null | tr -d ' ')
                  if [ "${_sz:-0}" -gt 0 ] && [ "${_sz:-0}" -le 200 ]; then
                      echo "file $f: $(head -1 "$UNION$f" 2>/dev/null)"
                  else
                      echo "file $f: present ${_sz} bytes"
                  fi
              else
                  echo "file $f: absent"
              fi
          done

          echo "### TESTKIT END"
      ) >&2 2>/dev/null || true
