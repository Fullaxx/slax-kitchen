apiVersion: slax-kitchen/v1
kind: Profile
metadata:
  name: browsers
  summary: Every browser with a 64-bit Debian package, on a UEFI-bootable hybrid ISO
base:
  flavour: debian
  arch: 64bit
  version: "12.2.0"
recipes:
  # REMOVAL IS ITS OWN RECIPE, AND GOES FIRST. A bundle is built against everything below
  # it, so removing one afterwards can strand it; check_plan_order refuses that order
  # before anything is modified. Nothing else here removes anything, so the rest of this
  # list composes in any order.
  - name: remove-bundle
    vars: {drop: "^05-chromium\\.sb$"}
  # Do NOT add chromium-current, firefox-esr or debian-browsers: the first two are
  # superseded by this bundle and the third is its alternative, so a second browser bundle
  # would reinstall what this one already carries.
  - all-browsers
  # testkit prints facts about the ASSEMBLED filesystem to the serial console just before
  # change_root, which is what lets the boot test assert on more than "it booted". With
  # nine declared packages plus their whole dependency closure, the merged dpkg count is
  # the single number here most likely to be wrong.
  - testkit
  - serial-console
  - isohybrid
  # uefi-bootable builds the GRUB menu by parsing isolinux.cfg, so it runs after anything
  # that adds a menu entry. Keep it last.
  - uefi-bootable
output:
  name: "slax-browsers-{{version}}.iso"
  hybrid: true
test:
  - structure
